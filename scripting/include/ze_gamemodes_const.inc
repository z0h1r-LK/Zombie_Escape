#if defined _ze_gamemodes_const_included
	#endinput
#endif
#define _ze_gamemodes_const_included

/**
 * Gamemodes return value.
 */
enum (+=1)
{
	ZE_GAME_INVALID = -1,
	ZE_GAME_CONTINUE,
	ZE_GAME_IGNORE
}